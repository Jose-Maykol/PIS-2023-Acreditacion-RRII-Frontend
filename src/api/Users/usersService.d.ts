declare module '@/api/Users/usersService.js' {
  import { AxiosResponse } from 'axios'
  // import { APIResponse } from '@/types/apiTypes'
  // import { User } from '@/types/userTypes'

  // export class UsersService {
  // 	listUsers: () => Promise<{
  //     status: boolean;
  //     data: {
  //       status: boolean;
  //       message: string;
  //       data: UserData[]
  //     }
  //   }>

  // 	listUsers(): Promise<{ status: boolean; data: any }>;
  // 	detailUser(): Promise<{ status: boolean; data: any }>;
  // 	createUser(params: {
  //     email: string;
  //     role_id: number;
  //     access_token: string;
  //   }): Promise<{ status: boolean; data: any }>;

  // 	updateUser(id: number, params: {
  //     role_id: number;
  //     registration_status_id: number;
  //   }): Promise<{ status: boolean; data: any }>;

  // 	deleteUser(id: number): Promise<{ status: boolean; data: any }>;
  // 	enableUsers(): Promise<{ status: boolean; data: any }>;
  // }

  // const instance: UsersService
  // export default instance
}
